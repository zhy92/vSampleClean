export default {
  busintypeid: [{ required: true, message: "请输入编码", trigger: "blur" }],
  busintypename: [{ required: true, message: "请输入名称", trigger: "blur" }],
  businid: [{ required: true, message: "请输入编码", trigger: "blur" }],
  businname: [{ required: true, message: "请输入名称", trigger: "blur" }],
  reserved: [{ required: true, message: "请选择是否预留", trigger: "change" }],
  sysType: [{ required: true, message: "请选择系统类型", trigger: "change" }]
};
